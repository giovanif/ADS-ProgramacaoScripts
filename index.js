// Chamar a biblioteca express
const express = require('express');

// Armazenar na varável server a biblioteca express()
const server = express();

// Chamar a função listen para "escutar a porta que vamos utilizar"
server.listen(3500);